#ifndef INPUTDEVICES_HPP__
#define INPUTDEVICES_HPP__

/** An enumeration which provides all the input devices the application can handle. */
enum EInputDevices
{
	evInputDeviceWiiMote,
	evInputDeviceKinect,

	evInputDeviceNone,
};

#endif
